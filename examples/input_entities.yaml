# Beispiel: definiere in configuration.yaml oder via UI diese Input Entities
input_number:
  manual_meter_input:
    name: 'Manuelle Ablesung'
    initial: 0
    min: 0
    max: 10000
    step: 0.01

input_button:
  send_manual_reading:
    name: 'Ablesung senden'
